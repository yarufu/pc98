# ADV98 script.txt 内部仕様メモ

## 目的
このファイルは公開マニュアルではなく、実装確認用の内部メモ。
ユーザー向け説明は以下を参照。
https://pokug.net/entry/2026/05/03/233421

## 一覧

| 命令           | 用途             |
| ------------ | ---------------- |
| `#bg`        | 背景変更           |
| `#bgwipe`    | 背景ワイプ表示      |
| `#bginterlace` | 背景インターレース表示 |
| `#left`      | 左立ち絵変更       |
| `#leftwipe`  | 左立ち絵ワイプ表示  |
| `#leftinterlace` | 左立ち絵インターレース表示 |
| `#right`     | 右立ち絵変更       |
| `#rightwipe` | 右立ち絵ワイプ表示  |
| `#rightinterlace` | 右立ち絵インターレース表示 |
| `#choice`    | 可変長選択肢開始   |
| `#endchoice` | 可変長選択肢終了   |
| `#label`     | ジャンプ先ラベル   |
| `#jump`      | ラベルへ移動     |
| `#call`      | サブルーチン呼び出し |
| `#return`    | 呼び出し元へ復帰 |
| `#set`       | フラグON        |
| `#reset`     | フラグOFF       |
| `#if`        | フラグONならジャンプ |
| `#ifnot`     | フラグOFFならジャンプ |
| `#setnum`    | 数値代入         |
| `#add`       | 数値加算         |
| `#ifeq`      | 数値一致条件分岐   |
| `#ifge`      | 数値以上条件分岐   |
| `#pal`       | パレット読み込み   |
| `#bgm`       | BGM読み込み＆再生 |
| `#bgmstart`  | BGM再開        |
| `#bgmstop`   | BGM停止        |
| `#bgmfade`   | BGMフェードアウト |
| `#se`        | FM効果音再生      |

---

## FM効果音仕様

ADV98.EXE では、PMD の FM効果音機能を使用します。

FM効果音データは、実行ファイルと同じフォルダに配置した `SE.EFC` から読み込みます。

`#se <番号>` で0～127のFM効果音を再生します。引数が範囲外、数値以外、
または `SE.EFC` が読み込まれていない場合は、何もせずゲームを続行します。

```
#se 2
```

効果音番号 1 はシステム効果音として使用されています。

## 背景インターレース表示

`#bginterlace <背景名>` は、640×400全体のG98背景をインターレース表示します。
偶数ラインは上から下へ、奇数ラインは下から上へ段階的に表示されます。

背景名には `.g98` を付けません。

```
#bginterlace classroom
```

演出後は、GameStateに保存されている現在の左右立ち絵を再描画します。
セーブ／ロード後は通常背景として復元され、インターレース演出は再実行されません。

## 立ち絵インターレース表示

`#leftinterlace <キャラ番号> <表情>` と
`#rightinterlace <キャラ番号> <表情>` は、立ち絵SPRファイルを1回openし、
32ライン固定バッファへ描画帯ごとに読み込みながら、横方向の走査線を段階的に増やして表示します。

既存の `#left` / `#right` / `#leftwipe` / `#rightwipe` とは別命令です。

```
#leftinterlace character01 normal
#rightinterlace character02 happy
```


詳細はココ見る
https://pokug.net/entry/2026/05/03/233421
